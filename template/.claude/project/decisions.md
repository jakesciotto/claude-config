# decisions.md

Locked design decisions and the reasoning behind them. Do not re-litigate these; if a decision needs to change, record the reversal explicitly.

Project-specific decisions belong here, not in the global `~/.claude/rules/memory-decisions.md`, which is loaded into every session.
